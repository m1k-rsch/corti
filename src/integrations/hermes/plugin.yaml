name: corti
version: 0.1.0
description: "Corti memory provider for Hermes Agent. Supports both vendor-hosted Corti servers and local self-hosted instances with user-supplied LLM / embedding / rerank endpoints."
pip_dependencies:
  - httpx
