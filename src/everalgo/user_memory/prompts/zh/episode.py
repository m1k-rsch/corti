"""Chinese prompts for EpisodeExtractor.

Constants:
    - ``DEFAULT_CUSTOM_INSTRUCTIONS`` — 注入 {custom_instructions} 位的默认指令。
    - ``EPISODE_GENERATION_PROMPT`` — 通用 retrieval-optimised 情节生成；占位符：
      ``{conversation_start_time}`` / ``{conversation}`` / ``{custom_instructions}``。
    - ``USER_EPISODE_GENERATION_PROMPT`` — 以特定 ``{user_name}`` 为中心的变体；
      在以上三个占位符之外**额外**需要 ``{user_name}``。:class:`EpisodeExtractor` 在 `sender_id` 非 None
      时使用；传入 `sender_id=None` 会回退到通用 `EPISODE_GENERATION_PROMPT`。

输出 schema（两个变体一致）：``{"title": str, "content": str}``。
"""

DEFAULT_CUSTOM_INSTRUCTIONS = """
生成情节记忆时请遵循以下原则：
1. 每个情节应该是一个完整、独立的故事或事件
2. 保留所有重要信息，包括姓名、时间、地点、情感等
3. 使用陈述性语言描述情节，而不是对话格式
4. 突出关键信息和情感变化
5. 确保情节内容便于后续检索
"""

EPISODE_GENERATION_PROMPT = """
**关键语言规则**：你必须使用与输入对话内容**相同**的语言输出。若对话内容为中文，所有输出（标题和内容）**必须**为中文；若为英文，则输出英文。此规则强制执行。

你是一位情节记忆生成专家。请将以下对话内容转换为情节记忆。

对话开始时间：{conversation_start_time}
对话内容：
{conversation}

自定义指令：
{custom_instructions}

重要时间处理规则：
- "对话开始时间"标识本次对话的起始时间
- 每条消息都有自己的时间戳。任何非绝对时间表述都必须基于包含该表述的那条消息自身的时间戳解析为绝对日期，而非对话开始时间
- 示例："昨天""上周五""去年夏天""最近"——任何时间粒度均需解析
- 必须同时保留原始表述并附上解析后的绝对日期
- 格式："原始表述（绝对日期）"——例如"去年夏天（2022 年夏天）""上周五（2023-07-21）"
- 这种双重格式同时支持基于绝对时间和相对时间的提问

请生成结构化情节记忆，仅返回包含以下两个字段的 JSON 对象：
{{
    "title": "一段精炼、描述性的标题，准确概括主题（10-20 个词）",
    "content": "以第三人称叙述的精炼客观对话记录。必须涵盖所有重要信息：谁、何时参与、讨论了什么、做出了什么决策、表达了什么情绪、形成了什么计划或结果。按时间顺序组织，聚焦可观察的行为和直接陈述。删除冗余表达和啰嗦描述，同时保留所有事实、实体（姓名、日期、地点）和具体细节。保持精炼但不丢失关键信息。任何非绝对时间表述都必须基于该消息自身的时间戳解析为绝对日期，而非对话开始时间。"
}}

要求：
1. 标题应具体且易于检索（包含核心话题/活动）。
2. 内容必须包含对话中所有重要信息，同时保持精炼。
3. 将对话格式转换为叙事性描述。
4. 保持时间顺序和因果关系。
5. 除非明确为第一人称叙述，否则统一使用第三人称。
6. 包含有助于关键词检索的具体细节，特别是具体活动、地点和物品。
7. 任何非绝对时间表述都必须基于该消息的时间戳解析："原始表述（绝对日期）"——例如"去年夏天（2022 年夏天）"。
8. 描述决策或行动时，自然地融入其背后的理由或动机，但避免重复解释。
9. 一致使用具体人名而非代词，避免检索时的指代歧义。
10. 精炼性与冗余消除：
    - 删除冗余表达和啰嗦描述
    - 避免以不同方式重复相同信息
    - 消除不必要的填充词和短语
    - 保持句子直接而切题
    - 保留所有事实、实体（姓名、日期、地点）和具体细节
    - 维持核心含义和重要信息
    - 目标长度与原始对话相当或更短
11. 关键细节保留：
   - 人名：始终包含提及的所有人物全名（如"与小红的同事 Rob 一起做瑜伽"，而非"与一位同事做瑜伽"）
   - 专有名词与实体：所有专有名词、品牌名、地名、组织名应原文保留
   - 物品名称：包含具体的产品名、书名、电影名、餐厅名等
   - 数量与数字：精确记录所有数字、金额、价格、百分比、日期、时间（如"点了 3 份披萨"而非"点了披萨"）
   - 具体活动：使用精确的活动描述（如"做了高温瑜伽"而非仅"锻炼了"）
   - 时间点：包含所有提到的具体时间（如"下午 3:30""每周二""每周两次"）
12. 频次信息：
   - 记录重复发生的活动及其频次（如"每周二和周四去瑜伽课"）
   - 标注行为模式（如"在对话中提到三次给妈妈打电话"）
   - 包含习惯性行为（如"通常上班前 8 点喝咖啡"）
   - 记录重复次数（如"两次询问项目状态"）


示例：
如果对话开始时间为"March 14, 2024 (Thursday) at 3:00 PM UTC"，对话内容为 Caroline 计划徒步：
{{
    "title": "Caroline 的雷尼尔山徒步计划 2024-03-14：周末出行规划讨论",
    "content": "2024 年 3 月 14 日下午 3:00 UTC，Caroline 表示有兴趣本周末（2024 年 3 月 16-17 日）徒步并寻求建议。她想去雷尼尔山看日出。Melanie 询问装备时，Caroline 收到了以下建议：徒步靴、保暖衣物、手电筒、水和高能量食物。Caroline 决定周六凌晨（2024 年 3 月 16 日）出发以赶上日出，并计划邀请朋友。她对此次行程感到兴奋。"
}}

**关键语言规则**：你必须使用与输入对话内容**相同**的语言输出。若对话内容为中文，所有输出（标题和内容）**必须**为中文；若为英文，则输出英文。此规则强制执行。

仅返回 JSON 对象，不要添加任何其他文本：
"""


USER_EPISODE_GENERATION_PROMPT = """
**关键语言规则**：你必须使用与输入对话内容**相同**的语言输出。若对话内容为中文，所有输出（标题和内容）**必须**为中文；若为英文，则输出英文。此规则强制执行。

你是一位专业的事件记录员兼情节记忆生成专家，专注于从对话中为指定用户记录与其相关的关键事件。
你的任务是聚焦于 {user_name}，客观记录他/她所见、所闻、所言、所行，并将其转化为一段连贯、准确的事件记录。

用户名：{user_name}

对话开始时间：{conversation_start_time}
对话内容：
{conversation}

自定义指令：
{custom_instructions}

请遵循以下指导原则：

1.  **客观、中立的视角**：
    *   以事实为依据，核心是记录与 {user_name} 相关的事件，而不是将他/她塑造成故事主角。
    *   **严格区分角色**：如果 {user_name} 只是信息的接收者或提问者，记录必须清晰地反映这一点。例如，记录"{user_name} 了解到..."或"{user_name} 询问了关于...的情况"，而不是暗示他/她主导了讨论。
    *   过滤掉与 {user_name} 无直接关联的对话，但保留理解事件所必需的上下文。

2.  **连贯叙述**：
    *   将用户的关键行为和决策串联成一段流畅的叙述。例如，不要只写"{user_name} 同意了"，而是"在听取了大家的建议后，{user_name} 最终同意了该方案"，以体现上下文。
    *   清晰地展现 {user_name} 在事件中的角色和行为脉络。

3.  **提炼核心经历**：
    *   不仅是记录，更是提炼。总结出 {user_name} 在这次对话中的关键决策、重要计划、获得的承诺或遇到的问题。
    *   将这些核心信息自然地融入到叙述中。

4.  **忠于原文且易于理解**：
    *   所有内容必须基于对话原文，但可以用更具可读性的方式重新组织和表达。
    *   避免添加原文未提及的主观猜测或评价。

输出质量要求（在上述视角原则之上补充）：

5.  **精炼性与冗余消除**：
    - 删除冗余表达和啰嗦描述
    - 避免以不同方式重复相同信息
    - 保持句子直接而切题
    - 目标长度与原始对话相当或更短

6.  **关键细节保留**：
    - 人名：始终包含提及的所有人物全名（如"与小红的同事 Rob 讨论"，而非"与一位同事讨论"）
    - 专有名词与实体：所有专有名词、品牌名、地名、组织名应原文保留
    - 物品名称：包含具体的产品名、书名、电影名、餐厅名等
    - 数量与数字：精确记录所有数字、金额、价格、百分比、日期、时间（如"点了 3 份披萨"而非"点了披萨"）
    - 具体活动：使用精确的活动描述（如"做了高温瑜伽"而非仅"锻炼了"）
    - 时间点：包含所有提到的具体时间（如"下午 3:30""每周二""每周两次"）

7.  **频次信息**：
    - 记录重复发生的活动及其频次（如"每周二和周四去瑜伽课"）
    - 标注行为模式（如"在对话中提到三次给妈妈打电话"）
    - 包含习惯性行为（如"通常上班前 8 点喝咖啡"）
    - 记录重复次数（如"两次询问项目状态"）

8.  **时间引用——双重格式**：
    - 任何非绝对时间表述都必须基于包含该表述的那条消息自身的时间戳解析为绝对日期，而非对话开始时间
    - 示例："昨天""上周五""去年夏天""最近"——任何时间粒度均需解析
    - 必须同时保留原始表述并附上解析后的绝对日期
    - 格式："原始表述（绝对日期）"——例如"去年夏天（2022 年夏天）""上周五（2023-07-21）"

9.  **标题长度与前缀**：
    - 标题保持在 15-25 个词内
    - 包含下方 schema 所示的 `{user_name}` 前缀

10. **叙事形式**：
    - 全程使用第三人称（与原则 1 一致）
    - 保持时间顺序和事件之间的因果关系。注意：此处指**事件间**的因果（如"听取建议后→决定周六出发"），而非 {user_name} 在事件中的角色定位（角色归属仍由原则 1 管控）。

请生成结构化情节记忆，仅返回包含以下两个字段的 JSON 对象：
{{
    "title": "（关于`{user_name}`的[核心事件]的记录 YYYY-MM-DD）",
    "content": "（一段以`{user_name}`为中心的客观、连贯的叙事性文本。）"
}}

示例（错误风格，过于主观）：
{{
    "title": "{user_name} 领导了关于周末徒步的规划",
    "content": "{user_name} 发起了周末去雷尼尔山徒步的计划，并主导了关于装备的讨论。他/她最终拍板决定了出发时间，并安排了邀请朋友的事宜。"
}}

示例（正确风格，客观记录）：
{{
    "title": "{user_name} 参与规划周末雷尼尔山徒步 (2024-03-14)",
    "content": "{user_name} 在对话中提议本周末去雷尼尔山徒步，并询问了装备建议。在听取他人关于徒步靴、保暖衣物的建议后，{user_name} 决定于周六（2024-03-16）凌晨出发去看日出，并提到会邀请朋友同行。"
}}

自检清单：
- 返回的是否为严格的 JSON 格式？
- `title` 和 `content` 是否都严格围绕 `{user_name}` 展开？
- `content` 是否是一段流畅的经历叙述，而不是零散事实的堆砌？
- 所有关键信息是否都已自然地融入叙述中？

**关键语言规则**：你必须使用与输入对话内容**相同**的语言输出。若对话内容为中文，所有输出（标题和内容）**必须**为中文；若为英文，则输出英文。此规则强制执行。

仅返回 JSON 对象，不要添加任何其他文本：
"""
