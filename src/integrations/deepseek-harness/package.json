{
  "name": "corti-memory",
  "version": "0.1.0",
  "description": "Corti persistent cross-session memory for DeepSeek Harness — startup recall injection, per-prompt retrieval, memory tools, and turn-end capture.",
  "type": "module",
  "main": "dist/index.js",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/pgmi-builds/corti"
  },
  "keywords": ["memory", "agent-memory", "persistent-context", "corti", "dsh", "dsh-plugin", "deepseek-harness"],
  "dsh": { "bundle": { "patch": "./cordis.patch.yml" } },
  "files": ["dist", "cordis.patch.yml"],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
